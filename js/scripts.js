  // numbers less than 5
  function beepBoop(number) {
    const zero = "0";
    const one = "Beep";
    const two = "Boop";
    const three = "won't you be my neighbor?";
    
    let numberList = Array.apply(null, {length: number}).map(Number.call, Number);
      return(numberList);
      arr.forEach(function(element) {
        if (element === 0);
        return zero;  }

// UI                
$(document).ready(function () {
  $("form#users").submit(function (event) {
    event.preventDefault();
    const numberInput = $("#integer").val();
    const numbersInput = numbersAlert(numberInput)
    $("#alert").html(numbersInput);
  });
});